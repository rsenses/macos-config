# ████████╗███╗   ███╗██╗   ██╗██╗  ██╗
# ╚══██╔══╝████╗ ████║██║   ██║╚██╗██╔╝
#    ██║   ██╔████╔██║██║   ██║ ╚███╔╝
#    ██║   ██║╚██╔╝██║██║   ██║ ██╔██╗
#    ██║   ██║ ╚═╝ ██║╚██████╔╝██╔╝ ██╗
#    ╚═╝   ╚═╝     ╚═╝ ╚═════╝ ╚═╝  ╚═╝
# Terminal multiplexer
# https://github.com/tmux/tmux

# set-environment -g PATH "/opt/homebrew/bin:/bin:/usr/bin"

# Set terminal colors
#set-option -sa terminal-overrides ",xterm*:Tc"

# Start window and pane numbering from 1 for easier switching
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1

# Allow automatic renaming of windows
set -g allow-rename on

# Renumber windows when one is removed.
set -g renumber-windows on

# Allow the mouse to resize windows and select tabs
set -g mouse on

# use system clipboard
set -g set-clipboard on

# A bell in another window should cause a bell in the current window
set -g bell-action any

# don't detach tmux when killing a session
set -g detach-on-destroy off

# https://github.com/3rd/image.nvim/?tab=readme-ov-file#tmux
# This is needed by the image.nvim plugin
set -gq allow-passthrough on
# This is related to the `tmux_show_only_in_active_window = true,` config in
# image.nvim
set -g visual-activity off

# Increase history limit, as we want an "almost" unlimited buffer.
# May be set to something even higher, like 250k
set-option -g history-limit 100000

# Set vi mode
set-window-option -g mode-keys vi

#--------------------------------------------------------------------------
# Key Bindings
#--------------------------------------------------------------------------

# -r means that the bind can repeat without entering prefix again
# -n means that the bind doesn't use the prefix

# Set the prefix to Ctrl+Space
set -g prefix C-Space

# Send prefix to a nested tmux session by doubling the prefix
bind C-Space send-prefix

# Resize panes with Ctrl+Space {h,j,k,l}
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

# Split panes
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Change the path for newly created windows
bind c new-window -c "#{pane_current_path}"

# disable status bar
bind-key t set-option status

# STYLING
set -g pane-active-border-style bg=default,fg=color1
set -g pane-border-style fg=default

#--------------------------------------------------------------------------
# Sesh
#--------------------------------------------------------------------------
unbind f
bind-key "f" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' \
    --preview 'sesh preview {}'
)\""

bind-key x kill-pane # skip "kill-pane 1? (y/n)" prompt
bind -N "last-session (via sesh) " L run-shell "sesh last"

unbind N
bind-key N if-shell "tmux has-session -t notes" "switch-client -t notes" "new-session -s notes -c ~/doc/notes/ 'tmux rename-window 'notes'; nvim index.md ; tmux kill-session'"

# open lazygit in a new window with prefix+g
unbind g
bind-key g new-window -n lazygit -c "#{pane_current_path}" "lazygit"

# Open serpl search and replace in a new window with prefix+r
unbind r
bind-key r new-window -n serpl -c "#{pane_current_path}" "serpl"

# Open github links
unbind G
bind-key G run-shell "open-github.sh"

# Prefijo + s → nueva ventana SSH selector
unbind t
bind-key t new-window -n ssh "zsh -ic 'ss'"

#--------------------------------------------------------------------------
# Status line
#--------------------------------------------------------------------------

# zenbones colorscheme (mostly)
# ACCENT="#D6CDCA"
# BG="#EFEDEC"
# FG="#2C363C"
# kanso-pearl colorscheme (mostly)
ACCENT="#597B75"
BG="#F2F1EF"
FG="#22262D"

# Uncomment to move statusbar to the top
set-option -g status-position top

# Basic colors of the Statusbar
set-option -g status-style bg=default,fg=default

# Show the window list centered between the left and the right section
set-option -g status-justify left

# Style and set contents on the left section
set-option -g status-left "#[bg=${FG},fg=#{BG},bold]  #S "

# Style and set contents on the right section
set-option -g status-right "#[bg=${ACCENT},fg=#{BG}] #S "

# Set max length of left and right section
set-option -g status-left-length 100

# Style and set content for the inactive windows
set-option -g window-status-format "#[bold] #I:#W "

# Style and set content for the active windows
set-option -g window-status-current-format "#[bg=${ACCENT},fg=${BG}, bold] #I:#W "

# Style and set content for the message window
set-option -g message-style "fg=#{BG},bg=#{ACCENT}"

set-option -g mode-style "fg=#{BG},bg=#{ACCENT}"

#--------------------------------------------------------------------------
# TPM Plugins
#--------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# list of tmux plugins
set -g @plugin 'christoomey/vim-tmux-navigator' # for navigating panes and vim/nvim with Ctrl-hjkl
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

#--------------------------------------------------------------------------
# Restore sessions
#--------------------------------------------------------------------------

set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# if "test ! -d ~/.config/tmux/plugins/tpm" \
#    "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"
run '~/.config/tmux/plugins/tpm/tpm'
