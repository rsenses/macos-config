---
description: Create focused tests for the current plan before implementation
argument-hint: "[optional focus area]"
---

Use the `dev` and `ops` skills.

1. **Discovery**: Inspect existing tests and project conventions.
2. **Implementation**: Create or update focused tests for the agreed plan/behavior ($ARGUMENTS).
3. **Verification**: Run the narrowest relevant test command.
4. **Summary**: List tests added, behavior covered, and results.

*Note: Follow project patterns. Do not implement production code yet. Use `add_daily_note` to record progress.*
