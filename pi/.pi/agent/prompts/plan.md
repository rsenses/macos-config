---
description: Plan a task without modifying files
argument-hint: "<task>"
---

Use the `architect` and `ops` (scout/researcher) skills.

**Goal**: Create a technical plan for the task: "$ARGUMENTS"
**Rules**:
1. Read-only: Do not modify files.
2. Investigate: Use `scout` to find patterns and `researcher` for docs.
3. Structure: Follow the `architect` guidelines (Objective, Assumptions, Tasks with vertical slicing).
4. Output: Present the goal, discovered context, assumptions, and proposed tasks.
