---
description: Refine recently changed Laravel/PHP code for simplicity
---

Use the `ops` and `dev` skills.

Refine recently modified PHP/Laravel code for clarity and maintainability while preserving exact behavior.

**Rules**:
1. Only inspect/refine code touched in the current branch/session.
2. Preserve behavior exactly. No unrelated refactors.
3. Use framework-native patterns (Laravel). Avoid "clever" code.
4. Do not commit. Use `add_daily_note` to record meaningful refinements.

**Output**:
- Files changed and simplifications made.
- Targeted checks run and results.
- Risks or follow-ups.
