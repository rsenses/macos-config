# dev reload command

- Status: done
- Updated: 2026-05-11 00:00
- Scope: small

## Goal

Add `dev reload` so it performs a `down` followed by `up` for the current project/worktree.

## Current Plan

1. Inspect the current `bin/.local/bin/dev` command flow.
2. Add a `reload` subcommand that reuses the existing stop/start logic.
3. Update usage/help text.
4. Validate the script syntax.

## TODO

- [x] Implement `reload` subcommand
- [x] Update usage text
- [x] Validate with shell syntax check

## Decisions and Constraints

- Keep the change minimal and aligned with existing behavior.
- `reload` should operate on the current project by default, like `up`/`down`.

## Validation

- `bash -n bin/.local/bin/dev`

## Remaining / Next

- None.
