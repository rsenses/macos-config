# 2026-05-11

## Memory plan slug policy

- Updated memory extension so current session plan path uses `.ai/plan/YYYY-MM-DD-<session-id>-<short-slug>.md`.
- Extension now reuses an existing plan matching the current date/session id before suggesting a new slugged path.
- Updated memory skill, `/save-plan`, and project memory to document the session-id + slug policy.
- Validation: `node --check pi/.pi/agent/extensions/memory.ts`.

## dev reload command

- Added `dev reload` to `bin/.local/bin/dev`; it now runs `down` then `up` for the current project/name.
- Updated usage text and validated with `bash -n bin/.local/bin/dev`.
