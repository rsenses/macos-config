# 2026-05-18
- Fixed `bin/.local/bin/dev` so `dev up` falls back to plain `sudo caddy ...` when Caddy is down.
- Confirmed the site resolves to `127.0.0.1`; the issue was Caddy not running on 443, not DNS.
- Pending user validation that `dev up` now autostarts Caddy cleanly.
