# 2026-05-25

- **10:03**: Reviewed the new `pi-subagents` extension: the implementation looks solid and well-bounded; discoverability is decent for the model but not very obvious for humans. Suggested adding a clearer central trigger for when to use subagents.

- **10:05**: Added explicit subagent guidance to `pi/.pi/agent/APPEND_SYSTEM.md` with when-to-use, role mapping (scout/researcher/worker), and avoidance heuristics to improve discoverability.

- **10:06**: Strengthened `pi/.pi/agent/APPEND_SYSTEM.md` with a higher-salience default rule: for Medium or Serious tasks, prefer scout/researcher before editing.

- **10:58**: Aligned `pi/.pi/agent` skills/configs with the real `pi-subagents` set (`scout`, `researcher`, `worker`), removing stale references to `planner`, `delegate`, `reviewer`, `oracle`, and `context-builder`.

- **11:18**: Reviewed the Pi harness config for workflow minimalism and determinism. Main findings: the setup is strong on bounded subagents, plan/daily memory, and targeted tools, but still has drift risks from mixed model/provider config, a dead `pi/.ai` stub, and duplicated guidance across prompts/skills/memory.

- **11:44**: Added two memory-oriented tools to `memory.ts`: `get_current_plan` and `summarize_worktree`. Also kept the plan template and run/loop guidance more explicit but compact, to support smallcode-style control without adding new layers.
