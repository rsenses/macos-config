# 2026-05-20

- **12:57**: Compared the current Pi harness `ast_grep` tool with Elsin/pi-ast_grep; current tool is search/inspect/rule-focused with no replace, while Elsin's package adds a dry-run/apply replace wrapper around `sg run`.

- **13:09**: Created local Pi extension `pi/.pi/agent/extensions/ast-grep.ts` replacing `ast_grep` with pattern/rule/inspect plus dry-run/apply replace; smoke-tested via a temporary `ast_grep_test` name because the existing installed ast-grep still conflicts.

- **13:10**: Added improved local `ast-grep` skill at `pi/.pi/agent/skills/ast-grep/SKILL.md`, based on the package skill but updated for the new replacement-capable `ast_grep` extension and dry-run/apply workflow.

- **13:11**: After reload/removing the conflicting installed ast-grep, smoke-tested the local `ast_grep` extension directly: pattern, inspect, rule, and replace dry-run all execute successfully.

- **13:18**: Configured `pi-lsp-bridge` global fallback config at `pi/.pi/agent/lsp.json` with `phpantom_lsp` for PHP/Blade and `typescript-language-server --stdio` for TS/JS, marking vendor/node_modules as external.
