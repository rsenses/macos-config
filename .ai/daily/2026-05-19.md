# 2026-05-19

- **15:09**: Investigated tmux `prefix+f` sessionizer popup closing instantly; updated binding to launch `sessionizer` through a login zsh shell so the popup inherits the expected PATH.

- **15:11**: Added a temporary tmux debug bind (`prefix+Shift+f`) that writes PATH and `sessionizer` resolution to `/tmp/tmux-sessionizer.debug` so we can inspect why the popup exits immediately.

- **15:12**: Debugged tmux `prefix+f` sessionizer popup; confirmed the popup PATH omits `~/.local/bin`, so `sessionizer` wasn’t resolvable. Switched the binding to the absolute script path and kept a Shift+f debug bind.

- **15:51**: Reviewed the simplified pi harness config: it is cleaner conceptually, but still carries several packages/extensions and a few duplicate or legacy choices worth pruning.
