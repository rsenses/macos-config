# 2026-05-21

- **10:26**: Diagnosed pi-subagents exposing skills as agents: agent discovery recursively parses all `.md` under `~/.agents`, so `~/.agents/skills/*/SKILL.md` files with `name` + `description` frontmatter satisfy agent parsing and appear in `subagent list`.
