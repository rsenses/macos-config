# 2026-05-15

## pi-rtk investigation

- Verified `pi-rtk` is listed in both `pi/.pi/agent/settings.json` and `~/.pi/agent/settings.json`.
- Confirmed `rtk` is installed and reachable (`/opt/homebrew/bin/rtk`, `rtk 0.39.0`).
- Read `@sherif-fanous/pi-rtk` source: it rewrites only `bash` tool calls and `!<cmd>` shell commands; `!!<cmd>` is intentionally excluded.
- No session-log evidence yet of a load failure or missing package registration.
- Live validation suggests rewriting is active: a simple shell command came back in `ls -l`-style form after running through the tool.
