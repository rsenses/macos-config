# 2026-05-26

- **08:50**: Creé `bin/.local/bin/mariadb-import` para seleccionar con fzf archivos `.sql`/`.sql.gz` desde `~/data/work/docs/topics/general/sql` y luego elegir la base de datos local de MariaDB; validé la sintaxis con `bash -n`.

- **08:56**: Ajusté `bin/.local/bin/importdb` para listar siempre los archivos `.sql`/`.sql.gz` ordenados por fecha de modificación, con los más nuevos primero, y validé la sintaxis con `bash -n`.

- **08:59**: Añadí un indicador de progreso al importador `bin/.local/bin/importdb`: muestra un spinner/estado mientras corre la importación y reporta el tiempo total al terminar.

- **12:24**: Revisé `bin/.local/bin/tunnel-ssh.sh` y el LaunchAgent `~/Library/LaunchAgents/com.ruben.tunnel-ssh.plist`. Eliminé el `SSH_AUTH_SOCK` hardcoded del script/plist y alineé `~/.ssh/config` para que los hosts del túnel usen explícitamente `IdentityAgent /Users/rubensilvarodriguez/.bitwarden-ssh-agent.sock` en lugar de `IdentityFile` inexistentes. El problema visible era SSH auth/agent, no TablePlus.

- **13:14**: Añadí a memoria la norma de hacer backup antes de tocar configs importantes (ssh, launch agents, shell init). El problema de TablePlus sigue abierto; el bloqueo actual apunta a interacción SSH agent / demasiadas claves, no a la app.

- **13:37**: Ajusté `bin/.local/bin/importdb` para que fzf muestre solo el nombre del archivo (manteniendo la ruta completa oculta para la importación y el preview). Validé con `bash -n`.
