
## Memory extension POC

- Added `pi/.pi/agent/extensions/memory.ts`.
- Extension ensures project-local `.ai/MEMORY.md`, `.ai/plan/`, and `.ai/daily/` for each Pi working directory.
- Injects current memory contents plus minimal memory policy on `before_agent_start`.
- `agent_end` only ensures today's daily file exists; the model remains responsible for useful summaries.

## pi-mem review adjustments

- Reviewed `jo-inc/pi-mem` for design ideas.
- Kept this extension project-local and simpler than pi-mem.
- Adopted the useful low-risk idea of injecting today's and yesterday's daily logs for continuity.
- Removed `.ai/` from `.gitignore` because project memory should live in the repo.

## Prompt memory alignment

- Reviewed prompt templates under `pi/.pi/agent/prompts/`.
- Added `memory` skill usage to implementation/fix/test/finalize/save-plan/ship/code-simplifier flows.
- Marked read-only planning/preflight/checkpoint prompts to use loaded memory as context without writing memory files.
- Reworked `update-expertise` to update curated `.ai/MEMORY.md` and daily notes instead of a separate `.ai/expertise/` tree.

## Project context tools POC

- Added `pi/.pi/agent/extensions/project-context.ts` with two compact retrieval tools: `project_files` and `project_search`.
- Tools wrap `fd` and `rg` with conservative default excludes and output limits to reduce noisy discovery.
- Validated syntax with `node --check pi/.pi/agent/extensions/project-context.ts`.

## Project context read windows

- Enhanced `project_search` with compacted lines and `Suggested reads` entries pointing to `read_window` calls.
- Added `read_window` tool to inspect snippets around a line number or first pattern match without reading whole files.
- Validated syntax with `node --check pi/.pi/agent/extensions/project-context.ts`.

## Project context retrieval guidelines

- Updated `project_files`, `project_search`, and `read_window` prompt guidelines to prefer compact project-context tools over raw shell discovery.
- Added generic behavior-verification guidance: check definitions plus call sites, triggers, configuration, conditions, and channels before concluding runtime behavior.
- Kept the extension framework-agnostic.

## project_files query robustness

- Reviewed a real-project retrieval test for Maximo configuration discovery.
- Fixed `project_files` to use fd fixed-string matching and normalize wrapping `*` so queries like `*maximo*` do not fail regex parsing.
- Validation: `node --check pi/.pi/agent/extensions/project-context.ts`.

## Project context read discipline

- Tightened project-context guidelines so agents default to `read_window` after `project_search` instead of full-file reads.
- Added guidance to expand snippets or make additional targeted windows before falling back to full-file reads.
- Added generic test-search guidance to restrict searches with `kind='test'` or test-specific globs.
- Validation: `node --check pi/.pi/agent/extensions/project-context.ts`.
