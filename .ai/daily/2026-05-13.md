# 2026-05-13

## worker validation workflow

- Added explicit validation-policy guidance to `orchestrator`, `pragmatic-testing`, `run-plan`, and `fix-plan` so workers do not independently add tests or run broad suites by default.
- Validation: reread edited sections for consistency; `git diff --check` passed.

## source-driven / review opt-in

- Tightened `orchestrator`, `source-driven-development`, `code-review-and-quality`, `improve-codebase-architecture`, `run-plan`, and `fix-plan` so docs/review-heavy workflows stay opt-in for small changes and local patterns are preferred first.
- Validation: reread edited sections for consistency.

## memory persistence review

- Reviewed `pi/.pi/agent/extensions/memory.ts` and confirmed it only injects memory/tasks/daily context and creates blank daily files; it does not automatically persist MEMORY/TASKS content.
- Observation: the missing coverage is likely due to the prompt/model flow, not a hidden write path.

## memory persistence hardening

- Hardened `pi/.pi/agent/extensions/memory.ts`, `pi/.pi/agent/prompts/finalize.md`, and the memory skill wording to say persistence changes must be written, not just mentioned.
- Updated `.ai/MEMORY.md` and the active session plan to record the explicit-write lesson.
- Validation: reread the edited sections; `git diff --check` passed.
- Fixed a parse error in the loaded `memory.ts` extension by escaping literal markdown backticks inside the injected prompt text.
