# 2026-05-12

## Agent skills and prompts

- Added curated Addy-derived skills under `pi/.agents/skills` plus custom `pragmatic-testing`.
- Updated existing `memory`, `orchestrator`, and `ship` skills to route skills, preserve lightweight Spec/Contract in plans, avoid ceremony, and require explicit commit approval.
- Reviewed prompt templates under `pi/.pi/agent/prompts` to align with the new skills and replace stack-specific command defaults with project-native placeholders.
- Validation: checked skill frontmatter and searched for leftover Node/TS/PHP-specific command defaults.

## project_search fix

- Fixed `pi/.pi/agent/extensions/project-context.ts` so `project_search` handles leading-`-` and regex-invalid queries by falling back to literal ripgrep search.
- Validation: confirmed `rg -F -e '->mount('` matches a literal snippet.

## commit message workflow

- Tightened `ship`, `finalize`, and `checkpoint` prompts so commit-message guidance prefers the user's stated outcome and current session plan goal over low-level implementation wording.
- Validation: reread the edited prompts and updated project memory.

## run-plan prompt args

- Added `argument-hint` and `$ARGUMENTS` passthrough to `pi/.pi/agent/prompts/run-plan.md` so the user's extra prompt text can be forwarded during plan execution.
- Validation: reread the edited prompt and session plan note.

## project_search literal-dollar support

- Updated `pi/.pi/agent/extensions/project-context.ts` so `project_search` uses literal text by default, keeping PHP `$` snippets searchable without escaping, while still allowing explicit regex only when requested.
- Validation: reread the updated extension and confirmed direct `rg -F -e '$ARGUMENTS'` matches the prompts.

## project-context simplification

- Simplified `pi/.pi/agent/extensions/project-context.ts` by extracting search-result grouping and read-window pattern lookup helpers, keeping behavior the same but making the main tool handlers easier to scan.
- Validation: `git diff --check` passed; TypeScript CLI validation is blocked here by missing local module/type dependencies.
