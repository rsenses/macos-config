# 2026-05-14

## pi context warning notification

- Added `.pi/extensions/context-warning.ts` to show a red `error` notification after `agent_end` when context usage exceeds 65%.
- Updated session plan, task list, and memory with the behavior and validation notes.
- Validation: reviewed the extension and ran `git diff --check`.

## project_files path query quirk

- Confirmed `project_files` works for slash-free filename queries, but fails for queries containing `/` because it forwards them to `fd` as search patterns.
- Source checked: `pi/.pi/agent/extensions/project-context.ts`.

## extension location note

- Confirmed repo-local Pi extensions live under `pi/.pi/agent/extensions/`.

## project-context cleanup

- Updated `project_files` to support slash-containing path queries via `fd --full-path`.
- Tightened the tool guidance so directory listings are routed to bash `ls/find` instead of `project_files`.
- Validation: reviewed the edited section and ran `git diff --check`.

## project_search path fallback

- Added a fallback so slash-containing literal queries like `auth/split` can return matching file paths when content search finds nothing.
- Tightened `project_search` guidance to keep file-path lookup on `project_files` when that is the main goal.
- Validation pending: reload Pi and test the fallback in the UI.

## orchestrator prompt guidance

- Strengthened `run-plan` and `fix-plan` so they explicitly ask the agent to delegate bounded work through orchestrator when the task is multi-file or multi-step.
- Updated memory with the rule of thumb that a bare “use orchestrator” line is not enough for execution prompts.
- Validation: reviewed the prompt edits; `git diff --check` pending.

## project_search array-like query normalization

- Normalized simple one-item bracketed string queries like `['table']` before handing them to `rg`.
- Tightened the unwrap regex so it only accepts one quoted item.
- Verified the unwrap regex with a small `node` snippet.
- Validation: `git diff --check` passed.

## search tool comparison research

- Compared Pi-friendly options for a follow-up search tool: `pi-ast-grep`, `pi-rustdex`, `pi-fff`, plus external `ast-grep`/semantic-search tools.
- Early takeaway: keep the built-in search honest, and add a separate specialized tool for structural/semantic search instead of more heuristics in `project_search`.
- Likely split: `ast-grep` for syntax-aware queries, `pi-rustdex` for semantic/natural-language lookup, `pi-fff` only if fuzzy path/content ranking is the main pain point.
- Validation: web research only.

## global fd preference

- Added `pi/.pi/agent/APPEND_SYSTEM.md` with a minimal global preference to use `fd` over `find` when possible.
- Mirrored the preference in `.ai/MEMORY.md`.
