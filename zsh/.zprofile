# ~/.config/zsh/.zprofile
# typeset -gU path PATH

# PATHS ========================================
# (opcional) Depurar: ver rápidamente qué php ve un login shell
# print -rl -- "PATH=$PATH"
# command -v -a php
